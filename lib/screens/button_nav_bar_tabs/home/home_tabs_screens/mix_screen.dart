import 'package:flutter/material.dart';

class MixScreen extends StatelessWidget {
  const MixScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("mix Screen"),
      ),
    );
  }
}