import 'package:flutter/material.dart';

class PastryScreen extends StatelessWidget {
  const PastryScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Pastry Screen"),
      ),
    );
  }
}