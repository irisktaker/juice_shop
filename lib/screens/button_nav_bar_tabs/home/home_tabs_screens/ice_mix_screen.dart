import 'package:flutter/material.dart';

class IceMixScreen extends StatelessWidget {
  const IceMixScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("icemix Screen"),
      ),
    );
  }
}